{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "SqlDbConfig": {
    // Do not commit the secret if you have overriden this var locally
    "ConnectionString": "set as environment variable in launchsettings or as a command line argument when running docker from the command line"
  }
}
